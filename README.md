# CP (Cálculo de Programas) (Português)

Resolução de problemas de **programação funcional** utilizando técnicas de **Cálculo de Programas**, no âmbito da unidade curricular de **CP**.  
O trabalho segue uma abordagem de [**programação literária**](http://www.literateprogramming.com/), onde código e documentação coexistem no mesmo ficheiro.

Tanto o enunciado como as soluções encontram-se em Português no [relatório final](/CP/cp2526t/cp2526t.pdf).

## Membros do grupo
* [darteescar](https://github.com/darteescar)
* [luis7788](https://github.com/luis7788)
* [tiagofigueiredo7](https://github.com/tiagofigueiredo7)


## Dependências

Para compilar e executar os programas, é necessário ter instalado:

- **GHC (Glasgow Haskell Compiler)**
- **GHCi**
- **lhs2TeX**
- **LaTeX (pdflatex, bibtex, makeindex)**
- [**Docker**](https://docs.docker.com/engine/install/) (opcional, mas recomendado)

## Ambiente de Desenvolvimento (Recomendado)

Para evitar problemas de compatibilidade entre sistemas operativos e versões de software, recomenda-se o uso de **Docker**, tal como sugerido no enunciado. Para construir e correr o ambiente Docker:

```sh
docker build -t cp2526t .
```

Para executar o ambiente interativo:

```sh
docker run -v ${PWD}:/cp2526t -it cp2526t
```

O volume partilhado permite editar os ficheiros localmente e compilá-los dentro do container.

## Compilação do Relatório

Dentro do container, o PDF pode ser gerado fazendo:

```sh
make
```

## Execução do Código Haskell

Dentro do container, o código Haskell pode ser carregado no GHCi com:

```sh
ghci cp2526t.lhs
```

# CP (Algebra of Programming) (English)
Solving **functional programming** problems using **Calculus of Programs** techniques, in the scope of the **CP** course.
The project follows a [**literate programming**](http://www.literateprogramming.com/) approach, where code and documentation coexist in the same file.

Both the statement and solutions are in Portuguese in the [final report](/CP/cp2526t/cp2526t.pdf).

## Group Members
* [darteescar](https://github.com/darteescar)
* [luis7788](https://github.com/luis7788)
* [tiagofigueiredo7](https://github.com/tiagofigueiredo7)


## Dependencies
To compile and run the programs, you need to have installed:
- **GHC (Glasgow Haskell Compiler)**
- **GHCi**
- **lhs2TeX**
- **LaTeX (pdflatex, bibtex, makeindex)**
- [**Docker**](https://docs.docker.com/engine/install/) (optional, but recommended)

## Development Environment (Recommended)
To avoid compatibility issues between operating systems and software versions, it is recommended to use **Docker**, as suggested in the statement. To build and run the Docker environment:

```sh
docker build -t cp2526t .
```

To run the interactive environment:

```sh
docker run -v ${PWD}:/cp2526t -it cp2526t
```

The shared volume allows you to edit the files locally and compile them inside the container.

## Report Compilation
Inside the container, the PDF can be generated by running:

```sh
make
```

## Haskell Code Execution
Inside the container, the Haskell code can be loaded into GHCi with:

```sh
ghci cp2526t.lhs
```